# Eclipse Forge installer assets

The Windows installer uses deterministic first-party artwork generated from the
Eclipse Forge visual contract.

Regenerate the binary assets on Windows before packaging:

```powershell
npm run assets:brand
```

Do not replace these files with third-party artwork or unlicensed fonts.
